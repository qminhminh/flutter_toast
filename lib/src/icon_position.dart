/// Vị trí hiển thị icon trong toast
enum IconPosition {
  /// Icon ở bên trái (mặc định)
  left,
  
  /// Icon ở bên phải
  right,
  
  /// Icon ở trên cùng
  top,
  
  /// Icon ở dưới cùng
  bottom,
  
  /// Icon ở giữa (overlay trên text)
  center,
}

